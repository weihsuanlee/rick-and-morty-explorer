export const siteConfig = {
  name: "Rick & Morty Explorer",
  description:
    "Search, paginate, and inspect characters from the Rick and Morty universe.",
};

export type SiteConfig = typeof siteConfig;
